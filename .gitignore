node_modules/
package.json
package-lock.json
.DS_Store

# Firebase Admin service-account key (secret — used by scripts/rename-tenant.js)
serviceAccountKey.json
**/serviceAccountKey.json

# Local Firestore dumps from scripts/nest-under-tenants.js --backup.
# These contain every record, including the deceased's names and biographies.
scripts/backups/
